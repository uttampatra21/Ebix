import React from "react";
import Header from "./Header";

const AddCart = ({ product }) => {
  console.log(product);
  return (
    <div>
      <Header />
      cart
    </div>
  );
};

export default AddCart;
